{% for card in featured %}
{# <div class="bg-white p-2 rounded">
    <img src="{{ card.img_url }}" alt="{{ card.img_alt_text }}" class="">
    <div class="p-10">
        <h1 class="text-2xl font-bold py-2">{{ card.card_header }}</h1>
        <p class="bg-white text-sm">{{ card.card_desc }}</p>
        <a href="{{ card.link }}" class="py-2 mt-8 px-6 bg-green-500 inline-block rounded">Read More</a>
    </div>
</div> #}
<div class="bg-white mr-5 p-2 rounded">
    <img src="{{ card.img_url }}" alt="{{ card.img_alt_text }}" class="mb-4">
    <div class="p-5">
        <a href="{{ card.link }}" class="text-2xl">{{ card.header }}</a>
        {# <span class="text-2xl font-bold text-green-800">{{ card.card_header }}</span> #}
        <p class="bg-white text-sm text-black">{{ card.card_desc }}</p>
        {# <a href="" class="inline-block rounded py-2 px-3 mt-4 text-white bg-green-500 ">Read More</a> #}
    </div>
</div>
{% endfor %}